/*
package net.skyhcf.atmosphere.bukkit.commands.menu.disguise;

import com.google.common.collect.Maps;
import net.frozenorb.qlib.menu.Button;
import net.frozenorb.qlib.menu.pagination.PaginatedMenu;
import org.bukkit.entity.Player;

import java.util.Map;

public class SkinSelectionMenu extends PaginatedMenu {

    @Override
    public String getPrePaginatedTitle(Player player) {
        return "Select Skin";
    }

    @Override
    public Map<Integer, Button> getAllPagesButtons(Player player) {
        Map<Integer, Button> buttons = Maps.newHashMap();
        buttons.put(0, new SkinButton("Steve"));
        buttons.put(1, new SkinButton("Alex"));
        buttons.put(2, new SkinButton("Egirl"));
        buttons.put(3, new SkinButton("Eboy"));
        buttons.put(4, new SkinButton("Preceding"));
        buttons.put(5, new SkinButton("SoNot"));
        buttons.put(6, new SkinButton("SimplyFake"));
        buttons.put(7, new SkinButton("Idiot"));
        return buttons;
    }
}
*/
